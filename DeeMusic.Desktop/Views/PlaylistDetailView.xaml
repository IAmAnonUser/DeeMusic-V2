<UserControl x:Class="DeeMusic.Desktop.Views.PlaylistDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Playlist Header -->
            <Grid Grid.Row="0" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Playlist Cover -->
                <Border Grid.Column="0"
                       Width="240"
                       Height="240"
                       CornerRadius="8"
                       Background="{DynamicResource MaterialDesignDivider}">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" 
                                        ShadowDepth="4" 
                                        Opacity="0.3"/>
                    </Border.Effect>
                    <Image Source="{Binding Playlist.CoverUrl}" 
                          Stretch="UniformToFill"/>
                </Border>
                
                <!-- Playlist Info -->
                <StackPanel Grid.Column="1" 
                           Margin="32,0,0,0"
                           VerticalAlignment="Center">
                    
                    <TextBlock Text="PLAYLIST" 
                              FontSize="12"
                              FontWeight="SemiBold"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                              Margin="0,0,0,8"/>
                    
                    <TextBlock Text="{Binding Playlist.Title}" 
                              FontSize="32" 
                              FontWeight="Bold"
                              TextWrapping="Wrap"
                              Margin="0,0,0,8"/>
                    
                    <TextBlock Text="{Binding Playlist.Creator}" 
                              FontSize="16"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                              Margin="0,0,0,4"/>
                    
                    <TextBlock Text="{Binding Playlist.Description}" 
                              FontSize="14"
                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                              TextWrapping="Wrap"
                              MaxWidth="500"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,16"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                        <TextBlock Text="{Binding Playlist.TrackCount, StringFormat='{}{0} tracks'}" 
                                  FontSize="14"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <TextBlock Text=" • " 
                                  FontSize="14"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"
                                  Margin="8,0"/>
                        <TextBlock Text="{Binding Playlist.Duration}" 
                                  FontSize="14"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                        <TextBlock Text=" • " 
                                  FontSize="14"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"
                                  Margin="8,0"/>
                        <TextBlock Text="{Binding Playlist.FanCount, StringFormat='{}{0:N0} fans'}" 
                                  FontSize="14"
                                  Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    </StackPanel>
                    
                    <!-- Download Playlist Button -->
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                           Command="{Binding DownloadPlaylistCommand}"
                           Height="48"
                           Width="220"
                           HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Download" 
                                                   Width="24" 
                                                   Height="24"
                                                   VerticalAlignment="Center"/>
                            <TextBlock Text="Download Playlist" 
                                      Margin="8,0,0,0"
                                      VerticalAlignment="Center"
                                      FontSize="14"
                                      FontWeight="Medium"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
            
            <!-- Track Listing -->
            <materialDesign:Card Grid.Row="1" 
                               UniformCornerRadius="8"
                               Padding="0">
                <ListView ItemsSource="{Binding Playlist.Tracks}"
                         SelectionMode="Single"
                         Background="Transparent"
                         BorderThickness="0">
                    
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="16,12"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="40"/>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Track Number -->
                                <TextBlock Grid.Column="0"
                                          Text="{Binding TrackNumber}"
                                          FontSize="14"
                                          Foreground="{DynamicResource MaterialDesignBodyLight}"
                                          VerticalAlignment="Center"
                                          TextAlignment="Center"/>
                                
                                <!-- Album Cover Thumbnail -->
                                <Border Grid.Column="1"
                                       Width="40"
                                       Height="40"
                                       CornerRadius="4"
                                       Background="{DynamicResource MaterialDesignDivider}"
                                       Margin="8,0">
                                    <Image Source="{Binding CoverUrl}" 
                                          Stretch="UniformToFill"/>
                                </Border>
                                
                                <!-- Track Info -->
                                <StackPanel Grid.Column="2"
                                           VerticalAlignment="Center"
                                           Margin="8,0">
                                    <TextBlock Text="{Binding Title}"
                                              FontSize="14"
                                              TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Artist}"
                                              FontSize="12"
                                              Foreground="{DynamicResource MaterialDesignBodyLight}"
                                              TextTrimming="CharacterEllipsis"
                                              Margin="0,2,0,0"/>
                                </StackPanel>
                                
                                <!-- Album Name -->
                                <TextBlock Grid.Column="3"
                                          Text="{Binding Album}"
                                          FontSize="13"
                                          Foreground="{DynamicResource MaterialDesignBodyLight}"
                                          VerticalAlignment="Center"
                                          TextTrimming="CharacterEllipsis"
                                          MaxWidth="200"
                                          Margin="16,0"/>
                                
                                <!-- Duration -->
                                <TextBlock Grid.Column="4"
                                          Text="{Binding Duration}"
                                          FontSize="14"
                                          Foreground="{DynamicResource MaterialDesignBodyLight}"
                                          VerticalAlignment="Center"
                                          Margin="16,0"/>
                                
                                <!-- Download Button -->
                                <Button Grid.Column="5"
                                       Style="{StaticResource MaterialDesignIconButton}"
                                       Width="36"
                                       Height="36"
                                       Command="{Binding DataContext.DownloadTrackCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                       CommandParameter="{Binding}"
                                       ToolTip="Download Track">
                                    <materialDesign:PackIcon Kind="Download" 
                                                           Width="18" 
                                                           Height="18"/>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </materialDesign:Card>
        </Grid>
    </ScrollViewer>
</UserControl>
